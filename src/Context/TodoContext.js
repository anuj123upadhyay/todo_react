import { createContext } from "react";

export default createContext(null);// yee createContext hi vo parent banane main help karta hai jisme agar humne koi state persisit kari vo child main kanhi bhi acccessible hogi